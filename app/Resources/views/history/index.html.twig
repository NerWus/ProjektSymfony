{% extends '@PetkoparaCrudGenerator/base.html.twig' %}

{% block title %}
{{ parent() }} - file
{% endblock %}

{% block body %}

<div id="top" class="row">
    <div class="page-header">
        <h2>Download history <span aria-hidden="true"></span> </h2>
    </div>
</div>

<div class="row">
    <div class="col-md-4">
        <p><strong>Full file name</strong></p>
   

    </div>
    <div class="col-md-2">
        <p><strong>File Type</strong></p>
       

    </div>
    <div class="col-md-2">
        <p><strong>Creationg time </strong></p>
    

    </div>
    <div class="col-md-2">
        <p><strong>Subject</strong></p>
 

    </div>
    <div class="col-md-2">
        <p><strong>IP Address</strong></p>


    </div>
    </div>
<hr/>

{# <div class="form-group">
    <a class="btn btn-default" href="{{ path('files') }}">
        <span class="glyphicon glyphicon-list" aria-hidden="true"></span>
        Back to list
    </a>
    pcg_page
        <a class="btn btn-primary" href="{{ path('files_edit', { 'id':  file.id }) }}">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            Edit
        </a>
        <a class="btn btn-info" href="{{ path('files_new') }}">
            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
            New
        </a>
        <form action="{{ path('files_delete', { 'id': file.id }) }}" method="post" style="display: inline-block">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button class="btn btn-danger" type="submit" onclick="return confirm('Are you sure');">
                <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                Delete
            </button>
        </form>
</div> #}


{% endblock %}

